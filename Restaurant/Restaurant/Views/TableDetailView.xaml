<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Restaurant.Views.TableDetailView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:syncfusion="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
    xmlns:tabView="clr-namespace:Syncfusion.XForms.TabView;assembly=Syncfusion.SfTabView.XForms"
    x:Name="Root"
    Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <tabView:SfTabView BackgroundColor="WhiteSmoke" VisibleHeaderCount="2">
                <tabView:SfTabItem Title="Trạng thái">
                    <tabView:SfTabItem.Content>
                        <StackLayout Margin="10,20,10,0">
                            <StackLayout>
                                <syncfusion:SfRadioGroup>
                                    <syncfusion:SfRadioButton x:Name="radioButton" Text="Occupied" />
                                    <syncfusion:SfRadioButton x:Name="radioButton1" Text="Dirty" />
                                    <syncfusion:SfRadioButton x:Name="radioButton2" Text="Clean" />
                                </syncfusion:SfRadioGroup>
                            </StackLayout>
                        </StackLayout>
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>
                <tabView:SfTabItem Title="Đặt món">
                    <tabView:SfTabItem.Content>
                        <StackLayout VerticalOptions="StartAndExpand">
                            <ListView ItemsSource="{Binding Tests}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <StackLayout>
                                                <StackLayout.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding BindingContext.TappedCommand, Source={x:Reference Root}}" CommandParameter="{Binding .}" />
                                                </StackLayout.GestureRecognizers>
                                                <Label Text="{Binding .}" />
                                            </StackLayout>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <Button
                                Margin="50,0,50,5"
                                CornerRadius="5"
                                HorizontalOptions="CenterAndExpand"
                                Text="Submit"
                                VerticalOptions="End" />
                            <AbsoluteLayout>
                                <ImageButton
                                    Command="{Binding FABCommand}"
                                    AbsoluteLayout.LayoutBounds=".9,.5,60,80"
                                    AbsoluteLayout.LayoutFlags="PositionProportional"
                                    BackgroundColor="Transparent"
                                    HeightRequest="50"
                                    Source="ic_fab.png"
                                    WidthRequest="50" />
                            </AbsoluteLayout>
                        </StackLayout>
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>
            </tabView:SfTabView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>